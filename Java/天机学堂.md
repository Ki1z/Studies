# 天机学堂

`更新时间：2026-7-18`

注释解释：

- `<>`必填项，必须在当前位置填写相应数据

- `{}`必选项，必须在当前位置选择一个给出的选项

- `[]`可选项，可以选择填写或忽略

*注：该笔记内的可选项和参数均不完整，如有需要，请查询相关手册*

---

根据黑马程序员Java实战项目《天机学堂》，我们开始尝试搭建一套完整的微服务项目

## 项目介绍

### 初步认识

天机学堂是一款基于微服务架构的生产级在线教育项目。天机学堂致力于构建一个专业的职业教育网络在线学习平台，提供了如IT互联网、视觉设计、语言留学、考试考证等学习内容

> ![](javaweb2/236.png)

同时项目也提供了课程购买、优惠券、在线视频观看、视频留言与回复、创建学习计划等等功能，力求一个尽量真实的微服务项目

### 项目特色

从2021年国务院颁布《关于进一步减轻义务教育阶段学生作业负担和校外培训负担的意见》，即“双减政策”开始，多年来占据我国教育培训半壁江山的课外辅导培训机构遭到毁灭性打击，而相对地，职业教育培训的市场规模逐步增长

> ![](javaweb2/237.png)

而随着互联网发展，传统汗液也逐渐网络化，加之曾经的疫情影响，很多职业技能培训企业，以及高校课程都开始发展在线教育。相比于传统线下培训，在线教育成本更低，学习时间碎片化，教育资源能够充分利用，在岗人员也可能利用空间时间学习新知识新技能。因此，在线教育市场不断扩大，我们选择了天机学堂作为学习项目

> ![](javaweb2/238.png)

而天机学堂不仅仅涵盖在线职业教育方面，而是同时包含电商、在线教育、社交互动的核心业务，如对接多种支付接口，优惠券系统设计方案、库存超卖问题方案、秒杀抢购解决方案、超时订单处理方案，以及视频点播方案、学习辅助系统、考试测评系统、视频发布、审核、加密方案、视频断点续传方案，还有互动问答系统、课程评价系统、通用点赞实现方案、积分系统实现方案、排行榜实现方案等等

而在项目技术栈中，可以分为纵向四层

- 展现层：安全防护技术XSS-CSRF、VUE、WebPack、Axios、Pinia、Vue-Router、Element-UI、Echarts、Sass
- 接入层：NGINX、CDN、身份认证、请求路由、负载均衡
- 服务层：SpringCloud、LoadBalancer、OpenFeign、Nacos、Seata、Sentinel、RabbitMQ、XXL-JOB、Redisson
- 数据层：MySQL、MongoDB、Redis、Elasticsearch

此外，还有第三方云服务如阿里云、腾讯云，以及DevOps组件YAPI、GOGS、Jenkins、Docker、GrayLog、SW

## 项目环境搭建

### 企业开发模式

微服务项目相比传统项目，包含的项目模块非常多，每个模块都需要独立部署，因此在开发模式上有很大差别，开发人员数量成倍增多，分组分别开发不同的微服务模块，微服务模块间有业务关联，需要相互协作

而我们当前的天机学堂项目开发情况如下

> ![](javaweb2/239.png)

其中绿色勾表示已经开发完成，黄色圆圈表示正在开发中，红色叉表示暂未开发

而我们在企业中开发时，不同的服务代码一般位于不同的仓库中，作为某一个微服务的开发人员，我们是没有权限拉取和推送代码到其他仓库的。而且微服务的运行环境非常复杂，比如先前的黑马商城项目，我们需要同时启动Nacos、Sentinel、Seata、Redis、MySQL、RabbitMQ等等，因此本地部署的难度也较高

但是如何进行远程调用测试呢？在企业开发中，针对测试有不同的测试阶段：

- 单元测试：测试最小的可测试单元
- 集成测试：验证某个功能接口
- 组件测试：验证微服务组件
- 端对端联调：验证整个系统

单元测试和集成测试是在本地完成的，如果需要测试远程调用，一般企业会准备一套远程公共Mock服务来测试远程调用接口，也就不需要其他尚未开发完成的实际接口

因此本次项目的很多通用环境已经配置到了虚拟机中，只需要启动虚拟机，就可以一键启动并配置各种环境

> ![](javaweb2/240.png)

然后我们访问虚拟机IP，192.168.150.101，这个IP尽量不要修改，因为Nacos中可能已经设置了这个IP

> ![](javaweb2/241.png)

### 持续集成环境

不难发现，在上文的虚拟机中，并没有启动已经开发完成的微服务，这里就需要引出持续集成环境。持续集成是一种DevOps软件开发实践，DevOps中Dev为Develop，Ops为Operation，即开发与运维，也就是让开发与运维间更加流畅，打破开发与运维间的隔阂。例如我们在开发完成后，需要将代码进行打包、发布、编译、构建、部署等等，如果人为操作就非常麻烦，而且也及其容易出错

而持续集成可以自动构建、发布、测试，同时防止主干和分支存在较大差异，快速发现错误，并解决错误。而天机学堂项目中，也引入了持续集成环境，分别是Jenkins，负责编译，构建，测试代码；Gogs，负责代码托管，也就是Git私服；以及Docker，负责部署，Docker可以利用dockerfile和docker-compose快速部署。在这套持续集成环境中，只需要程序员在IDE中点击提交按钮，代码由Gogs接管，然后通知Jenkins拉取并编译、构建，然后打包为Docker镜像，最后Docker自动部署，程序员只需要关注中间步骤是否出错，以及访问最后运行的容器

我们可以手动来测试一下，访问Jenkins，可以看到已经提前准备好的一些微服务

> ![](javaweb2/243.png)

点击右侧的按钮就可以直接编译并一键部署，我们尝试启动tj-gateway，然后观察Docker

> ![](javaweb2/244.png)

可以看到，网关服务直接编译成功并自动部署在Docker中了，而在实际企业开发中，也不需要我们手动按下这个按钮，只需要在IDE中提交代码即可。同时，我们可以访问后端swagger接口文档

> ![](javaweb2/245.png)

 可以看到，也能够正确读取到接口内容。所以后面的学习中，我们将会利用这一套持续集成环境来进行开发，提升我们的开发效率

### 本地开发部署

先前我们将代码保存到了Git私服中，现在就需要将代码从仓库中拉取到本地，然后进行开发。首先进入Gogs，查看项目地址。注意选择lesson-init分支，不要选择master分支，master分支是全部开发完成的分支

> ![](javaweb2/246.png)

然后通过Git拉取代码，使用-b lesson-init选择课程分支

```bash
git clone http://192.168.150.101:10880/tjxt/tianji.git -b lesson-init
```

> ![](javaweb2/247.png)

然后以lesson-init为起点，创建一个dev分支

```bash
cd tianji
git checkout -b dev
```

> ![](javaweb2/248.png)

用IDEA打开项目，确认分支为Dev分支

> ![](javaweb2/249.png)

## BUG修复

### BUG复现

修复BUG是熟悉一个项目的最佳方式，想要修复好一个BUG，就必须清楚了解一个项目的整体架构，实现细节，业务流程，因此我们也从修复一个BUG开始了解天机学堂项目

现在已知一个业务BUG，我们登录jack的账号，删除其中一个订单

> ![](javaweb2/250.png)

然后登录rose的账号，也尝试删除一个订单，却提示不能删除他人的订单

> ![](javaweb2/251.png)

### BUG修复

很明显这里存在一个业务BUG，可能的原因有两点，第一，删除订单时真的获取到了别人的订单id，所以导致鉴权失败；第二，鉴权出现问题，获取订单id正确，但是在判断订单归属时出现错误，导致后台认为该订单属于其他用户

不过，解决BUG并不是目的，解决这个BUG的最终目的是学会阅读项目代码、学会定位问题、分析问题，学会发布代码到开发环境，以及熟悉项目结构

#### 熟悉项目

对于项目结构，目前企业微服务开发项目结构有两种模式

- 每个项目下的每一个微服务都需要创建一个Project，尽可能降低耦合
- 每个项目创建一个Project，项目下的微服务是Project下的Module，方便管理与开发

而天机学堂采用第二种模式

#### 配置文件规范

SpringBoot配置文件支持多环境配置，在天机学堂中也基于不同环境有不同的配置文件

| 文件                | 说明                                                     |
| ------------------- | -------------------------------------------------------- |
| bootstrap.yml       | 通用配置属性，包含服务名称、端口、日志等等各环境通用信息 |
| bootstrap-dev.yml   | 线上开发环境配置属性，在虚拟机中部署使用                 |
| bootstrap-local.yml | 本地开发环境配置属性，本地开发、测试、部署使用           |

除此之外，其余的配置都放在了Nacos中共享，包括共享的配置及其微服务中可能会根据业务变化的配置

#### 阅读源码

阅读他们的代码，一般有一个统一流程，找到请求入口、理清请求链路、紧跟主线、步步深入。从前端的请求入口开始，然后开始通过入口接口的代码，分析代码的执行流程，很多业务通常会执行多个分支业务，所以需要寻找业务主线，最后找到BUG发生点

现在我们来模拟最麻烦的一种情况，即原代码开发人员已经不参与该项目开发，我们只能通过自己来解决BUG问题

首先访问前端，在前端发送一次请求，查看请求路径

> ![](javaweb2/252.png)

可以看到，请求路径为api.tianji.com/ts/orders/1597502678241378305，已知api.tianji.com为服务网关入口，Nginx可能也会通过api.tianji.com来进行转发，所以最重要的请求路径并不是api.tianji.com，而是/ts/orders/1597502678241378305

因此第二步，观察网关中/ts前缀的请求会转发到哪个微服务

```yml
gateway:
  routes:
    - id: ms
      uri: lb://media-service
      predicates:
        - Path=/ms/**
    - id: as
      uri: lb://auth-service
      predicates:
        - Path=/as/**
      filters:
        - PreserveHostHeader
    - id: ds
      uri: lb://data-service
      predicates:
        - Path=/ds/**
    - id: sms
      uri: lb://message-service
      predicates:
        - Path=/sms/**
    - id: us
      uri: lb://user-service
      predicates:
        - Path=/us/**
    - id: cs
      uri: lb://course-service
      predicates:
        - Path=/cs/**
    - id: os
      uri: lb://order-service
      predicates:
        - Path=/os/**
    - id: ss
      uri: lb://search-service
      predicates:
        - Path=/ss/**
    - id: ls
      uri: lb://learning-service
      predicates:
        - Path=/ls/**
    - id: ps
      uri: lb://pay-service
      predicates:
        - Path=/ps/**
    - id: ts
      uri: lb://trade-service
      predicates:
        - Path=/ts/**
    - id: es
      uri: lb://exam-service
      predicates:
        - Path=/es/**
    - id: rs
      uri: lb://remark-service
      predicates:
        - Path=/rs/**
    - id: prs
      uri: lb://promotion-service
      predicates:
        - Path=/prs/**
```

从配置文件中得知，/ts/\*\*对应的微服务为trade-service交易微服务，因此移步到交易微服务，原始请求为/ts/orders/1597502678241378305，/ts/用于网关转发，所以在交易微服务中，我们只需要寻找DELETE方式，请求路径为/orders/的方法即可

```java
@ApiOperation("删除订单接口")
@DeleteMapping("/{id}")
public void deleteOrder(@ApiParam("要删除的订单id") @PathVariable("id") Long id) {
    orderService.deleteOrder(id);
}
```

跟踪进入orderService.deleteOrder(id)

```java
@Override
public void deleteOrder(Long id) {
    // 1.获取登录用户
    Long userId = UserContext.getUser();
    // 2.查询订单
    Order order = getById(id);
    if (order == null) {
        return;
    }
    // 3.判断订单所属用户与当前登录用户是否一致
    if(userId != order.getUserId()){
        // 不一致，说明不是当前用户的订单，结束
        throw new BadRequestException("不能删除他人订单");
    }
    // 4.删除订单
    boolean success = removeById(id);
    if (!success) {
        throw new DbException(OPERATE_FAILED);
    }
}
```

从上往下看，首先获取登录用户，然后查询订单，再判断订单所属用户与当前登录用户是否一致。这里就出现了我们在测试时遇到的报错throw new BadRequestException("不能删除他人订单")。所以问题一定出现在if(userId != order.getUserId())，这也与我们最初的推测一致，要么删除订单时真的获取到了别人的订单id，所以导致鉴权失败；要么获取订单id正确，但是在判断订单归属时出现错误，导致后台认为该订单属于其他用户。这里只要userId或者order.getUserId()任意一个获取错误，都会导致报错

但仔细思考一下，哪一个出错的概率更大呢？order.getUserId()来源于我们用订单id获取到的订单实体中的用户id，理论上来说，这里出错的概率应该比较小才对，如果order.getUserId()都出错了，则说明数据库在保存订单数据时就出错了，订单id无法正确对应用户id。但从另一个角度上来说，我们登录不用的用户，却又能获取到不同的订单数据，这与前面的推测矛盾。所以我们将目光着重于userId，userId来自于Long userId = UserContext.getUser()，我们进行跟踪

```java
package com.tianji.common.utils;

public class UserContext {
    private static final ThreadLocal<Long> TL = new ThreadLocal<>();

    /**
     * 保存用户信息
     * @param userId 用户id
     */
    public static void setUser(Long userId){
        TL.set(userId);
    }

    /**
     * 获取用户
     * @return 用户id
     */
    public static Long getUser(){
        return TL.get();
    }

    /**
     * 移除用户信息
     */
    public static void removeUser(){
        TL.remove();
    }
}
```

可以看到UserContext仅仅是利用ThreadLocal保存了用户信息，UserContext.getUser()本身也不存在什么问题。那么问题出在哪儿呢？重新模拟一下BUG出现的过程。首先jack进行登录，然后由UserContext.getUser()获取到jack的id，再使用订单id获取订单实体，用订单实体中的用户id与jack的Id进行比较，比较通过，因此删除成功；然后rose进行删除，UserContext.getUser()先获取用户id。这里就不难发现，UserContext.getUser()获取的到底是jack的id，还是rose的Id呢？接下来不再进行推测，而是直接进行测试验证

将虚拟机中的trade停止，使用我们本地的trade服务，并检查Nacos是否检测成功

> ![](javaweb2/253.png)

然后进行调试，在if(userId != order.getUserId())处断点，先使用jack删除订单

> ![](javaweb2/255.png)

userId为2，order.getUserId()也为2，因此jack可以删除成功。然后测试rose

> ![](javaweb2/256.png)

奇怪的现象出现了，这里获取的userId为129，order.getUserId()也是129，理论上来说应该等于才对，但是程序给出的结果是不等于。难道是!=符号的问题吗？我们来测试一下，定义两个Long，都设置为2L

```java
@Test
public void test() {
    Long a = 2L;
    Long b = 2L;
    System.out.println(a == b);
}
```

> ![](javaweb2/257.png)

然后将Long都设置为129L

```java
@Test
public void test() {
    Long a = 129L;
    Long b = 129L;
    System.out.println(a == b);
}
```

> ![](javaweb2/258.png)

#### 解决问题

结果显而易见，正是因为使用了基本比较符!=，所以导致比较出错。而出错的根本是Java对于数值包装类的设定。Java对整数包装类设计了缓存机制，默认缓存-128到127的整数包装类型，而\=\=或者!=比较符并不是数值比较，而是引用比较，在内存中，缓存外的包装类数值占用不同的内存空间，因此\=\=或者!\=比较的结果为false，导致程序出现BUG

我们可以跟踪到Long的valueOf方法

```java
@HotSpotIntrinsicCandidate
public static Long valueOf(long l) {
    final int offset = 128;
    if (l >= -128 && l <= 127) { // will cache
        return LongCache.cache[(int)l + offset];
    }
    return new Long(l);
}
```

这里执行了一个判断，当传入的long l在-128到127之间时，会直接返回LongCache.cache数组中的值，否则返回一个新对象

```java
private static class LongCache {
    private LongCache(){}

    static final Long cache[] = new Long[-(-128) + 127 + 1];

    static {
        for(int i = 0; i < cache.length; i++)
            cache[i] = new Long(i - 128);
    }
}
```

问题已经溯源，解决方法也很简单，使用Objects类中的equals方法，它首先会进行==比较，然后还会调用对象自身的equals方法来比较，而Long的equals方法如下

```java
public boolean equals(Object obj) {
    if (obj instanceof Long) {
        return value == ((Long)obj).longValue();
    }
    return false;
}
```

调用了longValue()方法，longValue()返回的是一个long基本类型，因此可以保证比较结果正确。所以，我们将源代码进行更正

```java
// 3.判断订单所属用户与当前登录用户是否一致
if(!Objects.equals(userId, order.getUserId())){
    // 不一致，说明不是当前用户的订单，结束
    throw new BadRequestException("不能删除他人订单");
}
```

### 测试部署

代码开发完成的测试流程一般为本地单元测试、本地接口测试、组件测试以及开发环境联调。但是这里我们只修改了一行代码，因此就不进行本地单元测试，而直接进行接口测试

**本地接口测试**

从数据库中选择一个rose的订单进行删除，这里选择订单号1597502678241378305

> ![](javaweb2/259.png)

然后在请求头中添加user-info头，设置为rose的用户id129

> ![](javaweb2/260.png)

发送请求，返回200，并且没有返回任何结果，说明删除成功

> ![](javaweb2/261.png)

刷新数据库，可以看到id为1597502678241378305的订单已经被成功删除了

> ![](javaweb2/262.png)

**组件测试**

我们在本地启动修复好的服务，停用远程服务，然后访问前端再次进行删除测试

> ![](javaweb2/263.png)

可以看到，订单已经删除成功了，说明在组件测试中，BUG已经被彻底修复了，最后我们再尝试提交到仓库中进行部署

**部署测试**

在push代码之前，我们先修改Jenkins中有关构建部署的脚本，在Jenkins的tjxt-dev-build脚本中，默认的Branches to build为lesson-init分支，但是我们将分支修改为了dev分支，所以Jenkins中也需要修改分支

> ![](javaweb2/264.png)

然后修改构建条件，添加分支过滤器，只有当dev分支提交时才进行构建

> ![](javaweb2/265.png)

保存完成，然后我们在idea中提交代码

> ![](javaweb2/266.png)

提交完成后进行推送，这里的警告是安全性警告，可以忽略

> ![](javaweb2/267.png)

推送完成，在Jenkins可以看到构建进度条，第一次构建比较久，需要下载各项依赖，耐心等待即可

> ![](javaweb2/268.png)

构建完成，我们关闭本地的服务，然后测试开发环境的服务是否存在问题

> ![](javaweb2/269.png)

可以看到，在开发环境也没有任何问题，因此可以认为，BUG已经被修复

## 业务开发

### 一般流程

现在我们对天机学堂项目已经有了一定的掌握，所以可以进行初步的开发工作。但在代码编写之前，我们还需要做一些准备工作

- 分析产品原型

基于产品原型和需求，分析业务流程，抽取业务实体，设计业务接口

- 设计数据结构

根据业务实体和接口，设计数据库表以及接口关联的相关实体

- 实现功能接口

基于之前定义的接口用代码实现相关功能，如有必要，也可以对接口进行调整

- 测试

接口开发完成后先做本地测试，确保每一个接口基本可用且满足业务需求

- 联调

接口开发完毕后与其他微服务及前端联调，修复BUG

### 我的课程

#### 分析产品原型

目前“我的课程”页面为空，tj-learning服务也只有项目框架，没有任何代码

> ![](javaweb2/270.png)
>
> ![](javaweb2/271.png)

所以我们从“我的课程”页面出发，开始逐步进行项目开发作业。首先观察网页产品原型，分析页面内容

> ![](javaweb2/272.png)

在产品原型中，页面被分为上下两部分，上半部分是一个正在学习的课程，右侧提供了一个“全部课程”的按钮，对于正在学习的课程，提供了正在学习和课程评价两个按钮，下半部分是学习计划，学习计划为一个表格，展示本周的学习的计划。在这个页面中，从上到下需要获取的数据有，当前正在学习的课程及其课程数据、已经学习课时、正在学习对象、本周学习计划及其详细内容、本周积分奖励等等

点击全部课程后显示全部已购课程，可以按照学习时间和购买时间进行排序，这里还可以创建学习计划，继续学习、重新学习

> ![](javaweb2/273.png)

课程存在过期时间，对于已经过期的课程，仅提供删除课程选项

> ![](javaweb2/274.png)

对于右上角的创建学习计划按钮，点击后弹出创建学习计划窗口，然后选择已购买课程，输入每周学习章节数，自动计算预计完成时间

> ![](javaweb2/275.png)

对于课程自身的创建学习计划或者修改学习计划按钮，无法选择课程名称，其他与之前相同

> ![](javaweb2/276.png)

#### 设计业务接口

在分析了产品原型后，我们对“我的课程”页面有了基本了解，下一步就是设计业务接口。前端需要通过业务接口来请求数据，后端也需要根据接口来实现业务逻辑，并将数据封转后发送给前端

这里我们使用Apifox来设计并维护接口，例如我们设计一个分页查询课程接口，首先创建项目，命名为天机学堂

> ![](javaweb2/277.png)

然后创建分组，命名为 课程相关接口

> ![](javaweb2/278.png)

##### 分页查询课程

接着就开始定义接口，查询接口，请求方式肯定为GET，请求路径暂且设定为/lessons/，因为是分页查询，所以使用page作为标注，组合起来就是/lessons/page。而请求参数中，页码、每页大小必不可少，其他的参数根据页面原型确定

> ![](javaweb2/273.png)

在页面原型中，没有其他过滤字段，但是有根据学习时间和购买时间进行排序的功能，因此在请求参数中，还需要加入排序字段，同时需要设置排序方式。因此我们设计四个请求参数page、pageSize、sortBy和isAsc

> ![](javaweb2/279.png)

然后设计返回值，首先是分页查询必须的总条数和总页数，然后根据页面原型分析其他必要数据。首先是课程名，然后是一个过期时间，过期时间可以由创建时间和过期时间两部分来确定，如果过期时间为空，则认为永久有效；接着是学习状态，页面原型中定义了四个学习状态

> ![](javaweb2/280.png)

然后还需要学习进度，包含已经学习课时和总学习课时。以上是页面中显示的字段，但是我们还需要考虑关联信息需要包含关联字段，例如课程信息，我们需要提供课程id，这样前端可以通过课程id直接访问对应的课程；以及在实际前端中，课程可能对应了课程封面，所以还需要准备一个课程封面字段。同时仔细观察可以发现，针对每个课程还有一个学习计划按钮，而且按钮分为“创建学习计划”和“修改学习计划”两种状态，因此还需要一个字段记录学习计划状态

> ![](javaweb2/281.png)

除此之外，“我的课程”页面其实还有很多接口需要我们去分析，这些接口不仅限于http请求接口，还包括微服务调用的接口，如下

| 接口描述                           | 请求方式 | 请求路径                  |
| ---------------------------------- | -------- | ------------------------- |
| 支付或报名成功后，立即加入课表     | MQ通知   |                           |
| 分页查询我的课表                   | GET      | /lessons/page             |
| 查询我最近正在学习的课程           | GET      | /lessons/now              |
| 根据id查询指定课程的学习状态       | GET      | /lessons/{courseId}       |
| 删除课表中的某课程                 | DELETE   | /lessons/{courseId}       |
| 退款后，立即移除课表中的课程       | MQ通知   |                           |
| 校验指定课程是否是课表中的有效课程 | GET      | /lessons/{courseId}/valid |
| 统计课程学习人数                   | GET      | /lessons/{courseId}/count |

##### 添加课程到课程表

用户购买课程后，交易服务会通过MQ通知学习服务，学习服务将课程加入用户课表中

由于是通过MQ发送的消息通知，所以这个接口没有http请求方式，也没有请求路径，我们想要获取其通知必须参考MQ的RoutingKey，在一般企业中可以直接询问支付服务开发者，而我们模拟最坏情况，通过查看源代码来获取。这里就不需要再通过前端来判断，因为课程的购买一定在tj-trade服务中，我们观察其Controller

```java
@ApiOperation("免费课立刻报名接口")
@PostMapping("/freeCourse/{courseId}")
public PlaceOrderResultVO enrolledFreeCourse(@ApiParam("免费课程id") @PathVariable("courseId") Long courseId) {
    return orderService.enrolledFreeCourse(courseId);
}
```

在Controller中我们找到一个免费课立刻报名接口，这里肯定会完成整个下单流程，并且发送支付成功的消息，因此我们深入orderService.enrolledFreeCourse(courseId)

```java
@Override
@Transactional
public PlaceOrderResultVO enrolledFreeCourse(Long courseId) {
    Long userId = UserContext.getUser();
    // 1.查询课程信息
    List<Long> cIds = CollUtils.singletonList(courseId);
    List<CourseSimpleInfoDTO> courseInfos = getOnShelfCourse(cIds);
    if (CollUtils.isEmpty(courseInfos)) {
        // 课程不存在
        throw new BizIllegalException(TradeErrorInfo.COURSE_NOT_EXISTS);
    }
    CourseSimpleInfoDTO courseInfo = courseInfos.get(0);
    if(!courseInfo.getFree()){
        // 非免费课程，直接报错
        throw new BizIllegalException(TradeErrorInfo.COURSE_NOT_FREE);
    }
    // 2.创建订单
    Order order = new Order();
    // 2.1.基本信息
    order.setUserId(userId);
    order.setTotalAmount(0);
    order.setDiscountAmount(0);
    order.setRealAmount(0);
    order.setStatus(OrderStatus.ENROLLED.getValue());
    order.setFinishTime(LocalDateTime.now());
    order.setMessage(OrderStatus.ENROLLED.getProgressName());
    // 2.2.订单id
    Long orderId = IdWorker.getId(order);
    order.setId(orderId);

    // 3.订单详情
    OrderDetail detail = packageOrderDetail(courseInfo, order);

    // 4.写入数据库
    saveOrderAndDetails(order, CollUtils.singletonList(detail));

    // 5.发送MQ消息，通知报名成功
    rabbitMqHelper.send(
            MqConstants.Exchange.ORDER_EXCHANGE,
            MqConstants.Key.ORDER_PAY_KEY,
            OrderBasicDTO.builder()
                    .orderId(orderId)
                    .userId(userId)
                    .courseIds(cIds)
                    .finishTime(order.getFinishTime())
                    .build()
    );
    // 6.返回vo
    return PlaceOrderResultVO.builder()
            .orderId(orderId)
            .payAmount(0)
            .status(order.getStatus())
            .build();
}
```

其中最重要的便是第五步，发送MQ消息，通知报名成功，这里使用的交换机为order.topic，路由Key为order.pay，发送的数据体是一个OrderBasicDTO对象，深入对象

```java
package com.tianji.api.dto.trade;

import lombok.Builder;
import lombok.Data;

import java.time.LocalDateTime;
import java.util.List;

@Data
@Builder
public class OrderBasicDTO {
    /**
     * 订单id
     */
    private Long orderId;
    /**
     * 下单用户id
     */
    private Long userId;
    /**
     * 下单的课程id集合
     */
    private List<Long> courseIds;
    /**
     * 订单完成时间
     */
    private LocalDateTime finishTime;
}
```

可以看到，只要获取了消息中的下单用户Id和下单课程id，就可以将对应的课程添加到用户课表中。同时需要注意MQ可能会发布多条消息，需要保证业务幂等性

